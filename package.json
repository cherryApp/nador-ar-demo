{
  "name": "nador-ar-demo",
  "version": "1.0.0",
  "description": "Demo app for AR possibilities",
  "main": "index.js",
  "scripts": {
    "deploy": "firebase deploy --only hosting:cherry-test-site",
    "deploy:angular": "cd ionic && ng build && cd .. && npm run deploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bootstrap": "^5.3.2"
  }
}
